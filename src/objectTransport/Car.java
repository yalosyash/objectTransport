package objectTransport;

public class Car extends Transport{
	public Car(String name, float speed, int weight, String color, int[] coordinate) {
		super(name, speed, weight, color, coordinate);
	}
}
